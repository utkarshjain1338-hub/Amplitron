# https://docs.codecov.com/docs/codecovyml-reference

comment:
  layout: "newheader, reach, diff, flags, components, files, newfooter"
  behavior: default
  require_changes: false
  hide_project_coverage: false
